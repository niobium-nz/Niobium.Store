{
  "contentVersion": "1.0.0.0",
  "$schema": "https://azure.com",
  "parameters": {
    "environmentName": {
      "value": "${AZURE_ENV_NAME}"
    },
    "serviceBusQueueNames": {
      "value": "${SERVICE_BUS_QUEUE_NAMES}"
    },
    "customDomainName": {
      "value": "${CUSTOM_DOMAIN_NAME}"
    },
    "customDomainCertificateName": {
      "value": "${CUSTOM_DOMAIN_CERTIFICATE_NAME}"
    }
  }
}
